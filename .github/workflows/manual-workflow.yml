name: Manually run workflow

on: workflow_dispatch

jobs:
    job_1:
        runs-on: ubuntu-latest
        steps:
            - name: independent job
              run: echo independent job
    
    job_2:
        needs: job_1
        runs-on: ubuntu-latest
        steps:
            - name: dependent job
              run: echo dependent jon

